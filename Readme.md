# how to use Git
# install.packages("gitcreds")
# library(gitcreds)
# install.packages("usethis")
# library(usethis)
# go to github > settings > developer settings (at the bottom left)
# generate personal access token class
# select repo, user
# generate 